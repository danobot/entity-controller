# build:
#   stage: build
#   script:
#     - docker build -t test_container .
#     - docker run -v .:/app test_container
before_script:
  - docker info
test:
  script:
    - docker build -t test_container .
    - docker run --rm -v ${PWD}:/app test_container 
